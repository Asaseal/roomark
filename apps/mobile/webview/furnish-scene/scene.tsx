/*
 * Scene source note:
 * The current app uses plain Three.js inside sceneHtml.ts because it is the most stable
 * path for Expo WebView. React Three Fiber can replace this file later if the
 * scene is hosted as a bundled web app or remote static page.
 */

export {};
